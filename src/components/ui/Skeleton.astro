---
interface Props {
  class?: string;
}

const { class: className = "", ...rest } = Astro.props as Props & Record<string, unknown>;

const classes = [
  "animate-pulse rounded bg-terminal-cyan/20",
  className,
]
  .filter(Boolean)
  .join(" ");
---

<div class={classes} {...rest}></div>
